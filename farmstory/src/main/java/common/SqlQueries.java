package common;

public class SqlQueries {
	//public static final String SELECT_USER_BY_USERID = "SELECT * FROM users WHERE userId = ?";
	// ex 이런식으로 정리해서 dao에서 불러올때는 SqlQueries.SELECT_USER_BY_EMAIL이렇게 저장된 커리문을 불러줄 수 있음
	
	// select
	public static final String SELECT_USER_BY_USERID = "SELECT * FROM users WHERE userId = ?";
	// update
	
	// insert into
	
	// delete
}
